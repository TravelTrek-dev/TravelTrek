import 'package:flutter/material.dart';

Widget buildDescription() {
  return const Text(
    "Ascend to the top of the Iron Lady for panoramic views of Paris. Pre-booked summit access allows you to bypass the primary lines.",
    style: TextStyle(color: Color(0xFF6B6363), height: 1.5, fontSize: 14),
  );
}
